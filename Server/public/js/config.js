let baseUrl = "http://127.0.0.1";
let height = '600';
let cellMinWidth = 100;
let limit = 10;
let limits = [3, 5, 10, 15, 20];
let page = true;
let request = {
    pageName: 'page',// 页码的参数名称，默认：page
    limitName: 'size' //每页数据量的参数名，默认：limit
};

